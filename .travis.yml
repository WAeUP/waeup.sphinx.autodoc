language: python
sudo: false
python:
  - "2.7"
before_install:
  # nothing to do, currently
# command to install dependencies
install:
  - python setup.py dev
# command to run tests
script:
  - python setup.py test

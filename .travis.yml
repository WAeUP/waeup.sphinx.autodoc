language: python
sudo: false
python:
  - "2.7"
before_install:
  # nothing to do, currently
# command to install dependencies
install:
  - pip install tox
# command to run tests
script:
  - tox -e py27
